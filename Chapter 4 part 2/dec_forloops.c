#include<stdio.h>
int main() {
     /* we can also write the code as
     int i ;
     for(i =5; i ) and so on it is upto us */
    for(int i = 5 ; i ; i -- )   
    {                                       // Here also we dont need to type {}always when we have only a single line of code
        printf("%d is the value of i \n",i);
    }
  
 return 0;
}   // this code will print the values until zero